spring:
  application:
    name: zipkin-client-provider
  zipkin:
    base-url: http://localhost:7051

server:
  port: 7052

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7071/eureka/

logging:
  file: ${spring.application.name}.log